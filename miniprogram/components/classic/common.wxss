.classic-container{
  display: flex;
  /* 此时副轴从左到右 */
  flex-direction: column;
  align-items: center;
}

.classic-img{
  width: 750rpx;
  height: 500rpx;
}

.tag {
  width: 46rpx;
  height: 142rpx;
  /* 此时副轴从左到右 */
  align-self: start;
  margin-top: -56rpx;
  margin-left: 36rpx;
  margin-bottom: 100rpx;
}

.content {
  font-size: 36rpx;
  /*使得过大时有限制，较小时则自适应 仍可居中*/
  max-width: 550rpx;
  margin: auto 100rpx;
}

